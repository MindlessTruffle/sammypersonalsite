# Active medieval portfolio

Sammy chose this version over the mushroom cave. Work here on main, localhost:4174.
GitHub Pages hosting is now explicitly authorized for this project. Publish `docs/` from
main; regenerate with npm run build:pages and verify with npm run check:pages before pushing.
Sammy authorized sammyhawari.com. Preserve docs/CNAME and build public assets with root URLs.
Keep localhost:4174 and start-local.cmd usable. Do not switch hosting providers unasked.
The public build must never request a visitor's localhost: use the static shirt image and
project-details link online, and retain the live local preview only in dist/.

Keep three forest/fire/water cloth columns, readable body text, pixel headings, strong section
dividers, existing project info, actual brand logos, Neo Dev League award/organizer note, and
James Li attribution. Textured cloth must remain static: weave, worn threads, and subtle folds.
The WebGL shader is removed. Do not restore animated cloth or a full-page animal canvas.

Animals are small cached pixel sprites with cat swings, fox pounces, rabbit hops, and bird
flights. Suspend animation when hidden or behind the page portal, and respect reduced motion.
Keep purchase button design and quiet money orbit; retain arcade frame and social particles.
Social buttons use cached bird, binary, handshake, and signing-paper sprites beneath real logos.
Social scenes idle continuously at 24fps: banking birds, flowing binary, black-pen signing and wax-seal stamping.
Keep larger keys and both handles centered above the row (Discord: chatgpt9). Pause effects
offscreen, in dialogs, and for reduced motion; use static scenes when motion is reduced.
Use a compact rounded indie-game arrow cursor with a thick dark outline, cream fill,
mint link state, and brief pastel glitch accents. Starter flowers appear immediately but
share a four-second cooldown across all banners and strokes; never delay the vine itself.
Vines imprint immediately onto banner cloth, behind content, using fixed local coordinates.
Draw fresh vine geometry each display frame with no stem delay. Spaced paired leaves and
varied-color flowers grow after about one second, then fade within 3.4 seconds.
Keep contrast low and preserve unlimited stroke distance: expire by age, never by mark count.
Batch geometry writes and reuse leaf/flower symbols. Pause for hidden pages, dialogs, text
fields, or reduced motion. Keep elemental crests visible.

Internal project/note links open through a sword slash, one white flash, and splitting page
halves. Live subpage content appears in a native dialog between large red checkered close
buttons. Both buttons, Escape, and browser Back close it. Preserve real link URLs, modified
click behavior, direct-page loading, focus restoration, and reduced-motion alternatives.
Prefer cached sprites and temporary transition layers; no permanent screen snapshots.
The opening transition lasts two seconds: a 2D katana crescent, jagged tearing, then page expansion.
Use tapered slash VFX and a shared curved seam for both torn page edges; no traveling sword sprite.
Keep the article inert until the opening finishes, while Escape remains available. Clear pending
opening timers and temporary layers on interruption or a reduced-motion preference change.

Closing a submenu uses a dark cloth wipe with “thanks for checking me out :)”. It covers
the subpage before restoring the underlying page, holds the message briefly, and slides away.
Buttons, Escape, and Back share this transition. Preserve focus restoration, interruption
handling for Forward navigation, and immediate closing for reduced motion.

Keep purchase-orbit keyframes cached until the button resizes. Start the shirt preview only
when visible, avoid overlapping status requests, and pause polling behind any dialog. The
command menu dispatches the shared portfolio:overlay event for all decorative effects.

Fast pointer strokes use coalesced samples and cached cloth-boundary clipping so quick
sweeps remain continuous across sections without drawing through banner gaps. Offshoots are
spaced about 52px apart; stems remain immediate and leaf/flower growth stays delayed.
Keep vine/leaf opacity at 0.16 and flower opacity at 0.22. Use speed-adaptive pointer smoothing and midpoint curves to
absorb hand tremors without limiting fast sweeps. Ease the tail to a stopped pointer.
